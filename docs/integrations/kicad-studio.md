# KiCad Studio Integration

BoardGuard is the CI and pull request layer. KiCad Studio is the local IDE and VS Code layer.

Future integration may:

- run `boardguard scan` from a VS Code command
- consume BoardGuard JSON reports
- render findings beside schematic and PCB files

There is no hard dependency in v0.1.
