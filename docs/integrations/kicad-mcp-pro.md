# KiCad MCP Pro Integration

KiCad MCP Pro is the MCP and automation execution layer. BoardGuard is the repository validation and release gate.

Future integration may:

- call BoardGuard from MCP workflows
- consume BoardGuard JSON reports
- use BoardGuard as a quality gate before hardware release steps

There is no hard dependency in v0.1.
